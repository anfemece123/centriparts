# Production dependencies. NOT fully installable/verified in the sandbox
# this project was implemented in (no Docker, and torch/transformers are
# multi-hundred-MB downloads not reliably verifiable there) — see README.md
# "Estado de verificación" for exactly what was and wasn't run.
#
# For a lightweight environment that can actually run the test suite without
# a full ML stack, use requirements-test.txt instead (tests inject a dummy
# embedding provider and never import torch/transformers).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.12
pillow>=11.0,<12.0
numpy>=2.1,<3.0

# Embedding providers (DINOv2 / SigLIP), lazily imported — only downloaded
# and loaded into memory when that provider is actually selected.
torch>=2.4,<3.0
transformers>=4.46,<5.0

# Local OCR (spec section 17). Also requires the `tesseract-ocr` system
# package — installed in the Dockerfile, not pip-installable.
pytesseract>=0.3.13
